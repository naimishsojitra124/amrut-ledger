meta {
  name: Get Customer Bills
  type: http
  seq: 11
}

get {
  url: {{baseUrl}}/customers/{{customerId}}/bills
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{accessToken}}
  Accept: application/json
}

tests {
  test("Status is 200", () => {
    expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}