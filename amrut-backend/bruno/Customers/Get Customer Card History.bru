meta {
  name: Get Customer Card History
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/customers/{{customerId}}/card-history
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{accessToken}}
  Accept: application/json
}

tests {
  test("Status is 200", () => {
    expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}