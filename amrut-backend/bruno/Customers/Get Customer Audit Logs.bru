meta {
  name: Get Customer Audit Logs
  type: http
  seq: 13
}

get {
  url: {{baseUrl}}/customers/{{customerId}}/audit-logs
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{accessToken}}
  Accept: application/json
}

tests {
  test("Status is 200", () => {
    expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}