meta {
  name: Get Customer Card Assignment
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/customers/{{customerId}}/card-assignment
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{accessToken}}
  Accept: application/json
}

tests {
  test("Status is 200", () => {
    expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}