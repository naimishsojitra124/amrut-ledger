meta {
  name: Archive User
  type: http
  seq: 8
}

patch {
  url: {{baseUrl}}/users/{{createdUserId}}/archive
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{accessToken}}
  Accept: application/json
}

tests {
  test("Status is 200", () => {
    expect(res.getStatus()).to.equal(200);
  });

  const body = res.getBody();

  test("Archived", () => {
    expect(body.status).to.equal("inactive");
  });
}

settings {
  encodeUrl: true
  timeout: 0
}